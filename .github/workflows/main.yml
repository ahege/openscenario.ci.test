name: OpenSCENARIO Checker CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Checker Job
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ./checkOscAction
      with:
        CONF_FILE: ci/conf.yml
        BASE_DIR: targetFolder
        CHECKER_JAR_FILE: ci/net.asam.openscenario.checker.example-0.9.0.jar
        